apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tei.fullname" . }}-test-connection"
  labels:
    {{- include "tei.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tei.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
